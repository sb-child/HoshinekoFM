7.02-1版本测试报告


Bugs

1.(ok)启动时会打开两个仪表板标签页，应该是一个
2.(ok)如果在启动时不关闭错误开启的仪表板，可能导致Trap Pages*，表现为两个标签页的目录同步更新，并且你关掉任意一个另一个也会被关掉
3.(ok)多选文件无法拖给Localsend,表现为完全不接收（对方无反应）
4.(ok)似乎任何“打包”的文件都被归类成压缩包，我认为应该对压缩包、OpenOffice文档、MSOffice文档等在分类规则中特殊声明
5.使用打开方式打开的软件会丢失环境变量（疑似），但是双击打开无事


未完成常用功能/远期展望

1.希望可以通过把文件（框选多文件）拖动到标签页上来以复制（也许移动？）进另一标签页目录
2.希望可以把文件（框选多文件）拖进地址栏里的某个地址来复制到上级、上上级等目录
3.仪表板的“固定”功能应该从导入css改为导入文件和文件夹，并固定在那个区域
4.新建文件（夹）的对话框
5.在自定义终端中打开，实现方式可以是让程序去读取程序外的配置文件
6.在系统默认终端中打开
7.希望有内建调色板，或者能够继承dms的东西

